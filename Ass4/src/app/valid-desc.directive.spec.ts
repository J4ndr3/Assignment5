import { ValidDescDirective } from './valid-desc.directive';

describe('ValidDescDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidDescDirective();
    expect(directive).toBeTruthy();
  });
});
